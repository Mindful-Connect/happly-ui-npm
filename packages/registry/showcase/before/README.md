# Before snapshot

Verbatim copy of `packages/registry/{ui,lib,hooks}` at `production` @ b648434 (before the 2026-09-03 interface-skills sweep), with `@/` imports rewritten to relative paths. Used only by the Storybook showcase in `../skills-before-after.stories.tsx`. Not part of the registry; not linted; delete together with the showcase.
